<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>root page</title>
    </head>
    <body>
        <h1>{{pageTitle}}</h1>
        {{#each eventsList}}
            <p>{{#if start.dateTime}}{{start.dateTime}}{{/if}} - {{summary}}</p>
        {{/each}}

{{eventsList}}
    </body>
</html>
