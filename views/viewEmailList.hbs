<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>root page</title>
    </head>
    <body>
        <h1>{{pageTitle}}</h1>
        {{#each emailsList}}
            <p>{{from}} - {{subject}}</p>
        {{/each}}

{{eventsList}}
    </body>
</html>
